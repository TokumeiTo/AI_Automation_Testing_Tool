- python -m venv venv (A virtual environment (venv) acts like an isolated playground for Python. It ensures that the packages we install for this project (like FastAPI) don't mix with or break anything else on your machine.)
- .\venv\Scripts\activate
- backend>requirements.txt
fastapi>=0.100.0
uvicorn>=0.22.0
openai>=1.0.0
pydantic>=2.0.0
requests>=2.31.0

- pip install -r requirements.txt

- .\venv\Scripts\activate
- python -m uvicorn app.main:app --reload

- pip install python-dotenv (.env setup)
- pip install faker (The Faker library in Python is used to generate fake but realistic-looking data — useful for testing, seeding databases, or anonymizing sensitive information.)


- npm install @mui/material @emotion/react @emotion/styled @mui/icons-material
- npm install @playwright/test playwright
- npx playwright install (Install system browsers)
- npm install chalk





frontend/src/
├── apis/
│   └── aiApi.ts             # Direct Axios/Fetch backend integrations
├── components/
│   ├── InteractiveGrid.tsx  # The spreadsheet-like test step table
│   └── ScenarioInput.tsx    # The text input block with control triggers
├── pages/
│   └── TestWorkspace.tsx    # Page wrapper managing unified state orchestration
├── services/
│   └── playwrightMapper.ts  # Logic mapping steps directly into code views
├── App.tsx
└── main.tsx


1. Human + AI & Integration Scenario & TestCase Creation
2. Relational Test Data Generation
3. Test Execution & Evidence Taking
4. AI-Powered Mock Servers (Simulated Responses)
5. Automated Root Cause Escalation & Log Analysis
6. Reusable in Other Testing Phases (Unit, Integration, Comprehensive)




PORTABLE_PLAYWRIGHT
# Run this inside the client's backend/ms-playwright folder to restore the files
cmd /c "copy /b .\chromium-1228\chrome-win64\chrome.dll.part* .\chromium-1228\chrome-win64\chrome.dll"
cmd /c "copy /b .\chromium_headless_shell-1228\chrome-headless-shell-win64\chrome-headless-shell.exe.part* .\chromium_headless_shell-1228\chrome-headless-shell-win64\chrome-headless-shell.exe"

# Clean up the .part fragments so you don't waste disk space
Remove-Item ".\chromium-1228\chrome-win64\chrome.dll.part*"
Remove-Item ".\chromium_headless_shell-1228\chrome-headless-shell-win64\chrome-headless-shell.exe.part*"